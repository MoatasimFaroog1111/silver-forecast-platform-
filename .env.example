# Optional in local development. Defaults to runtime_state/silver_forecast.db
DATABASE_URL=sqlite:///./runtime_state/silver_forecast.db

# Railway injects PORT automatically.
PORT=8000

# Only needed when the frontend is hosted on a different origin.
CORS_ALLOWED_ORIGINS=
